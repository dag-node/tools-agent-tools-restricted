# Secret-name patterns for the ai-tools sandbox.
#
# Both root helpers read this file and treat a path whose BASENAME matches any
# pattern below as a credential file:
#   - ai-tools-chown  (PostToolUse/Stop handback) quarantines agent-written
#     matches to <you>:<you> 600, revoking ai-tools' read access.
#   - ai-tools-lockdown (manual `sudo ai-tools-lockdown` sweep) sets matches to
#     file 600 / dir 700, owner <you>:<you>, across the current project -- the same
#     owner the handback gives an agent-written secret.
#
# This file is owned <you>:<you> 600. You may edit it; ai-tools cannot read or
# write it (it is neither owner nor group, and cannot enter the 700
# .config/ai-tools directory) -- the root helpers read it on your behalf.
#
# Format:
#   - one pattern per line; '#' starts a comment; blank lines ignored
#   - patterns are BASENAME globs (no '/'), matched case-insensitively
#   - '*' matches any characters; '.' is literal
#
# Keep patterns anchored to a name or an environment segment. Avoid broad
# catch-alls like '*.*.json' / '*.*.config': those also match .NET build
# artifacts the toolchain must read (MyApp.deps.json, MyApp.runtimeconfig.json,
# project.assets.json, MyApp.dll.config), and quarantining them breaks builds.
#
# This file REPLACES the built-in defaults rather than adding to them: the helpers read the
# built-in list only when this file is removed or does not list any patterns. So an edit here is the
# whole set, and removing the file restores the defaults rather than your edits.
#
# This is where a name specific to your projects or organization belongs -- the built-in list
# is the public baseline and carries general conventions only. A credential name that software
# writes in general, and that the baseline misses, is worth a pull request upstream instead, so
# every host gets it.

# --- dotenv / environment ---
.env
.env.*
*.env
env
.envrc
.environment
.environment.*
environment

# --- generic secret / credential names ---
secret
secrets
usersecrets
private
secret.*
secrets.*
*.secret
*.credential
credential
credentials
credentials.*
password
passwords
password.*
passwords.*
apikey
apikeys
api_key
api_keys
*.apikey
token
tokens
.token
*.token

# --- SSH / private keys ---
id_rsa
id_dsa
id_ecdsa
id_ed25519
authorized_keys

# --- key / certificate / keystore material ---
*.ppk
*.pem
*.key
*.priv
*.p12
*.pfx
*.pkcs12
*.jks
*.keystore
*.p8
*.gpg
*.kdbx
*.ovpn
secring
secring.*
privkey
privkey.*

# --- tool credential files ---
kubeconfig
*.kubeconfig
.pgpass
.git-credentials
.dockercfg
.htpasswd
.npmrc
.pypirc
.netrc
.boto
.s3cfg
.my.cnf
my.cnf
.mylogin.cnf
.vault-token
vault_pass
vault_pass.*

# --- infrastructure-as-code state and variables ---
*.tfvars
*.tfstate
*.tfstate.backup

# --- cloud service-account keys ---
service-account.json
service-account-*.json
*-service-account.json
client_secret.json
client_secret_*.json
application_default_credentials.json

# --- deployment profiles carrying credentials ---
*.publishsettings
*.pubxml.user
*.mobileprovision

# --- .NET configuration carrying secrets (name-anchored) ---
connectionstrings.*.json
ConnectionString.*.config
commonsettings.*.json
CommonSettings.*.config
appsettings.*.json
AppSettings.*.config
web.*.config
App.*.config

# --- environment-qualified config (any extension) ---
*.DEV.*
*.STAGE.*
*.PROD.*
*.Development.*
*.Staging.*
*.Production.*
