# ai-tools integration manifest: typesafe, shipped by ai-tools-integration-typesafe.
# What the sandbox tooling needs to know about the TypeSafe decision integration: that a session
# gets it only where an operator names it, since a call sends listing lines off the host. Package
# data: read as KEY=value lines, not executed, and not edited on a host.
# Every key is described in ai-tools-providers(5).
default_enable=no
